# PiKVM Roadmap

A prioritized backlog of improvements for the PiKVM Go TUI + automation stack.

This list captures every idea we want to ship eventually. Items are grouped into **phases** by dependency order — Phase 1 must land before Phase 2 makes sense, etc. Inside a phase, items are independent and can be parallelized.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked / needs decision

---

## Phase 1 — Foundation (quick wins, no dependencies)

These are small, high-impact changes that make everything that follows easier.

### 1. `[x]` Live switch state via WebSocket — no polling, no `r` key
Replace `fetchSwitchState()` polling with a persistent `wss://<host>/api/ws` subscription. Stream switch / ATX LED / MSD / video-link / USB-link events into the Bubble Tea event loop as custom messages. The `[E]/[P]` rows update in real time when *anything* (this TUI, the web UI, your phone, a button on the device) changes the active port. The `r` key becomes a manual reconnect.

**Why first:** unlocks Phase 3's grid view, snapshot dashboard, and the status bar's "live" feel.

### 2. `[x]` Parallel + cached ISO listing
Run the PiKVM `/api/msd` query and the local `iso/` directory scan concurrently with `errgroup`. Memoize the result for ~30 seconds so reopening the ISO menu is instant. Foundation for caching snapshot thumbnails too.

### 3. `[ ]` Single config source: `config.json` generated by Vault
Stop having `pikvm.go`, `pikvm.sh`, and `automation/pikvm.py` each parse `.env` with three slightly different rules. `env-from-vault.sh` writes a single `config.json` (still gitignored). All three runtimes read it via stdlib JSON. One schema, one bug surface, one place to validate.

### 4. `[ ]` Cache HTTP client + auth header
Hoist `http.Transport`, `tls.Config`, and the basic-auth header into package-level singletons. Saves ~10 ms per call, prevents idle-connection leaks, simplifies every API function.

---

## Phase 2 — Per-port state + UI polish

### 5. `[ ]` Per-port profiles in `~/.config/pikvm/state.json`
Each port gets a JSON object: `name`, `bios_key`, `default_iso`, `tags`, `tailscale_name`, `ssh_user`. The TUI shows `[P] Port: [1] [2] [3] [4] ← j4yn0` instead of bare numbers, restores the last active port across PiKVM reboots, and eliminates the four hard-coded "Boot to BIOS (F7/Del/F2/F3)" entries.

**Why early:** Ideas 6, 7, 11, 13, 14, 22, 25 all build on this.

### 6. `[ ]` Smart "Boot to BIOS" — collapse 4 menu entries into 1
Use the per-port `bios_key` from idea 5. If unset, show a small key picker: `[F7] [Del] [F2] [F3] [F12] [Esc]`. Saves four menu lines, generalizes to any keystroke.

### 9. `[ ]` Dim / disable inapplicable actions
If `summary.synced == false`, dim port-dependent ops. If `power_leds[port] == true`, dim "Power ON" and bold "Power OFF". Reduces accidental no-ops and makes intent obvious.

### 10. `[ ]` Continuous status bar at the bottom
Single line: `pi@100.64.183.14 │ FW v9 │ 2 ext × 4 ports │ MSD: idle (1.4G free) │ uptime 4d12h │ ws ●`. Pulled from `/api/info`, `/api/msd`, and the WebSocket heartbeat. Live progress bar when an MSD upload is in flight. Instant "is the PiKVM alive?" feedback.

### 27. `[ ]` `--json` mode + `fzf` integration
Optional flag that emits structured events (`{"event":"port_changed","port":"2.3"}`) instead of pretty TUI output. Pipeable into `jq`, scripts, CI. Also: a `pikvm pick` mode that shells out to `fzf` for fuzzy selection of port / ISO / script — perfect for keyboard-only humans.

---

## Phase 3 — Big UX upgrades (need Phase 1 + 2)

### 7. `[ ]` Grid view of all ports (toggle with `g`)
Mosaic of every port across all extenders showing `power_state · video_link · usb_link · profile_name · is_active`. All data is already in `/api/switch`. Toggle from the standard view with `g`.

### 8. `[ ]` Snapshot thumbnails inline
`GET /api/streamer/snapshot?port=N` returns a JPEG. Render each port's snapshot at ~96×54 inside its grid cell using `chafa` (sixel / iTerm2 protocol) or a Go terminal-image library. Refresh every 5 s. A real PiKVM dashboard in the terminal — no browser required.

---

## Phase 4 — Architecture cleanup

Best done before any more big features land, because everything after this benefits.

### 16. `[ ]` Split `pikvm.go` into packages
```
internal/
  pikvm/      # PiKVM API client (switch, ATX, MSD, HID, snapshot, ws)
  config/     # .env / Vault loader, state.json
  scripts/    # built-in & user custom scripts (auto-registered)
  tui/        # bubbletea model, view, keymap
  cli/        # cobra-style subcommands
cmd/pikvm/    # main.go (just wires everything up)
```
Enables tests (currently zero coverage), enables mocking the HTTP client for golden-file TUI snapshots, makes the codebase readable.

### 17. `[ ]` Native Go for everything currently shelled out
Replace `osascript`, `gnome-terminal`, `pikvm.sh` callouts and `websocat` pipelines with in-process Go. Result: progress bars *inside* the TUI for ISO uploads, no popup terminal windows, no bash dependency for end users.

### 18. `[ ]` Single static binary distribution
With Phase 4 + 17 done, `go build -o pikvm` is the only step needed. Ship via `brew tap j4y/pikvm`. Zero `pip`, `venv`, `bash`, `python` for end users.

---

## Phase 5 — Profiles, multi-host, provisioning

### 11. `[ ]` Profile system: ports as machines
Builds on 5. CLI: `pikvm boot j4yn0 ubuntu-25.10` switches port + mounts ISO + spams BIOS key + types SSH bootstrap. `pikvm ssh vault` resolves a Tailscale name. `pikvm power on j4yn0` works without thinking in port numbers.

### 25. `[ ]` Multi-host federation
`~/.config/pikvm/hosts.yaml` lists every PiKVM you own (lab, garage, colo). TUI starts with a host picker. CLI is namespaced: `pikvm ssh garage:2.3`. Combined with Phase 3's grid view, this is a single pane of glass for an entire homelab.

### 15. `[ ]` PXE + ISO + Tailscale = one-shot bare-metal provisioning
Fuse `pxe/`, the ISO boot scripts, and `setupUbuntuServer`:
```
pikvm provision 2.3 --hostname web03
```
Switches port → power-cycles → PXE-boots Ubuntu autoinstall with generated `user-data` (hostname + Tailscale auth + SSH key) → waits for Tailscale join → SSHs in for post-install hooks → prints `✓ web03 ready: ssh j4y@web03`. Bare-metal to ready-to-use in ~10 minutes, hands-free.

---

## Phase 6 — Automation & scripting

### 12. `[ ]` Sequence runner in Go (replace Python + OpenCV)
Move the JSON sequence runner (`automation/seq-script/*.json`) into the Go binary. Use either [gocv](https://gocv.io) for OpenCV-quality template matching, or `corona10/goimagehash` (pure Go) for perceptual-hash similarity. Auto-discover sequences and surface them in `[C] Custom Scripts` — drop a `.json`, get a menu entry. No Python, no venv, no `pip install`.

### 14. `[ ]` Action recorder (mega-version)
Press `R`. The TUI starts capturing everything you do: every key (timestamped), every power button, every ISO mount, every port switch. **Plus**:

- **Screen recording** of the PiKVM HDMI stream (encoded H.264 to a `.mp4` per session)
- **Periodic snapshots** at decision points (every key press, every 2 s of idle)
- **OCR (`tesseract`)** on each snapshot to capture on-screen text alongside your inputs
- **Timing metadata** (how long you stayed on each screen)
- **AI post-analysis** — feed the recorded session to GPT-4V to produce a human-readable summary ("you booted the F7 menu, selected USB, waited 12s for grub, picked Ubuntu Live...") and an editable JSON sequence that can be replayed.

When you press stop, all of this lands in `automation/seq-script/recordings/<name>/` as a folder containing the JSON sequence, the MP4, the OCR text, and the AI summary. Replay by running it like any other script. Edit by hand if you want to tweak it.

### 20. `[ ]` Hooks for events
`~/.config/pikvm/hooks.d/` — drop in any executable. PiKVM events (`port-active-changed`, `msd-mounted`, `power-on`, `iso-upload-finished`, etc.) trigger matching hooks via `os/exec`. Use cases: Slack notifications on prod power-cycles, audit logs, terminal bell when a long upload finishes, integration with home-assistant.

---

## Phase 7 — Operability + scheduling

### 26. `[ ]` Scheduling engine (cron-style)
Per-port schedules: power on at 09:00 weekdays, power off at 22:00, mount X ISO at Y time. (Repurposed from "cost tracking" — the goal is *automation*, not penny-pinching, though kWh estimates are a free side benefit.)

### 19. `[ ]` Vault read on launch (deferred)
The Go binary auto-pulls fresh secrets from Vault when `config.json` is missing or stale. No more "I updated Vault but forgot to regenerate the config" drift. Deferred until Vault read is more painful than `env-from-vault.sh`.

---

## Phase 8 — AI + Web (long horizon)

### 13. `[ ]` Smart BIOS detection v1 (tesseract)
OCR the boot splash. "Press F2 to enter setup" → spam F2 instead of trial-and-error. Stepping stone to idea 22; ship as a fallback when the AI is unavailable or too slow.

### 22. `[ ]` AI BIOS pilot (GPT-4V / local llava-next)
Periodically feed snapshots to a vision model with a prompt like *"What screen is this and what should I press?"*. Auto-pick BIOS keys, detect Windows vs Ubuntu vs ESXi vs PXE menus vs panic screens, auto-recover stuck boots, type the right answer for "Press any key to boot from CD". Cost: ~cents per provision; saves hours of babysitting.

### 24. `[ ]` AI macro replay (idea 14 + idea 22)
Take a recorded session (idea 14). On replay, the AI watches the live screen and adapts the macro to small UI shifts ("the timezone picker is two pages further this version") instead of failing on a pixel mismatch. Result: macros that survive Ubuntu installer redesigns.

### 23. `[ ]` Multi-machine simultaneous control
Tile multiple PiKVM streams (across extenders or across hosts from idea 25) into one window. Keyboard focus follows `[1]/[2]/...`. Type the same command into N machines at once with a "broadcast" toggle. Useful for fleet operations like "run `apt upgrade` everywhere".

### 21. `[ ]` Embedded web UI (single binary, served by Go)
A small React/HTMX app served via `embed.FS`. Same backend as the TUI. Reach your PiKVM from your phone over Tailscale at `https://localhost:7443`. Grid view + power buttons + thumbnails. Could proxy the H.264 stream so you don't need PiKVM's heavyweight web UI for status checks.

---

## Cross-cutting concerns

These aren't features but should be addressed continuously:

- **Tests:** add Go test coverage as part of Phase 4. Golden-file TUI snapshots, HTTP-client mocks for the API.
- **Docs:** every shipped feature gets a section in `README.md` and (for big ones) its own page.
- **Telemetry-free observability:** structured logging (zerolog or slog) gated behind `--verbose`, never sent to a remote.
- **Backwards compatibility:** `config.json` and `state.json` schemas should version themselves so future migrations are clean.

---

## Sequencing summary

```
Phase 1: 1, 2, 3, 4              (foundations — start now)
Phase 2: 5, 6, 9, 10, 27         (state + UI polish)
Phase 3: 7, 8                    (grid + thumbnails)
Phase 4: 16, 17, 18              (architecture + single binary)
Phase 5: 11, 25, 15              (profiles + multi-host + provisioning)
Phase 6: 12, 14, 20              (automation + recorder + hooks)
Phase 7: 26, 19                  (scheduling + vault auto-pull)
Phase 8: 13, 22, 24, 23, 21      (AI + web)
```

Working order today: idea 1 → 2 → 3 → 4 (Phase 1).
